export const TOPIC_SUMMARIES: Record<string, string> = {
  "sequences-slicing": `Python slicing uses half-open intervals: \`seq[start:stop]\` includes the start index but excludes the stop index. This means \`a[:n]\` and \`a[n:]\` compose perfectly with no overlap or gap, and \`len(a[:n]) == n\` always holds. It is the same convention as \`range(n)\`. Under the hood, CPython normalizes omitted bounds and negative indices against the sequence length before performing the slice. For built-in sequences like lists, slicing allocates a new container and copies references for the selected range. The allocation is proportional to the slice length. Stride values like \`[::-1]\` change traversal order but still pay full allocation cost. Named slices via \`slice(0, 8)\` carry domain meaning better than bare \`[:8]\`. <a href="/sequences-pattern-matching">Compare with sequence pattern matching for structural branching</a>.`,

  "sequences-pattern-matching": `Sequence pattern matching, introduced in Python 3.10 (PEP 634), lets you branch on the shape of a subject instead of writing manual length and type checks. Instead of \`if len(seq) == 2 and isinstance(seq[0], str) and seq[0] == "move"\`, you write \`case ["move", x, y]\`. CPython implements this with three bytecode operations: \`MATCH_SEQUENCE\` verifies the subject is a sequence (excluding str, bytes, and bytearray), \`GET_LEN\` checks length, and \`UNPACK_SEQUENCE\` binds matched elements. If any check fails, the match continues to the next case. Guards with \`if\` add extra conditions after a pattern matches. Names are only bound on successful match, so unused names in earlier cases do not pollute the scope. <a href="/sequences-slicing">Review slice mechanics for related sequence operations</a>.`,

  "language-identity-equality": `Python's \`==\` and \`is\` answer different questions. \`==\` dispatches through \`__eq__\` and can be arbitrarily expensive, comparing value not identity. \`is\` compares object pointers directly and cannot be overloaded. When \`__eq__\` returns \`NotImplemented\`, Python interprets that as unsupported and tries the reflected operation on the other operand. If both return \`NotImplemented\`, the comparison falls back to identity. CPython interns small integers (typically -5 to 256) and some short strings as an optimization, but that is an implementation detail not a language guarantee. Use \`is None\` for singleton checks because \`None\` has a guaranteed single instance. Sentinel objects with \`object()\` follow the same pattern. Understanding aliasing explains why mutating one name can affect another. <a href="/language-mutable-defaults">See how aliasing creates the mutable default argument problem</a>. <a href="/language-dunder-methods">Learn how __eq__ and __hash__ work together</a>.`,

  "language-parameters": `Every function signature is a contract. Python gives you positional-only parameters before \`/\`, keyword-only arguments after \`*\`, and \`*args\` / \`**kwargs\` for variable arguments. Defaults are stored on the function object in \`__defaults__\` (a tuple) and \`__kwdefaults__\` (a dict), evaluated once at definition time. CPython 3.8+ uses vectorcall internally, which optimizes the calling convention by passing arguments in a C array instead of allocating intermediary objects. But the semantic model remains: \`*args\` collects extra positional values into a tuple, \`**kwargs\` collects extra keyword values into a dict. This page covers how to design signatures that make invalid calls structurally impossible, with examples for each parameter kind. <a href="/language-mutable-defaults">Watch out for shared mutable defaults when using default parameters</a>. <a href="/language-type-hints">Combine signatures with type hints for safer call sites</a>.`,

  "language-mutable-defaults": `The classic Python gotcha: \`def add_user(name, users=[])\` creates the empty list once at function definition time, not on each call. That list is stored in \`__defaults__\` and reused every time the caller omits that argument. All subsequent calls share and mutate the same list object. The fix is the sentinel pattern: use \`None\` as the default, check with \`is None\`, and create a fresh container inside the function body. This is not a Python quirk. It follows directly from how the language evaluates default arguments at definition time. The same issue affects dataclasses via \`field(default_factory=list)\`, and dict patterns like \`setdefault\` where the default value is evaluated eagerly. <a href="/language-parameters">Learn how Python evaluates and stores default arguments</a>. <a href="/dict-setdefault">See the same eager-evaluation issue in dict patterns</a>.`,

  "language-decorators-closures": `A decorator runs once at import time and replaces a function with another. When you write \`@cache\`, CPython executes the decorator call immediately during function definition. The closure is how the replacement remembers the original. CPython stores free variables in cell objects and reads them via \`LOAD_DEREF\` opcode, bridging scopes without copying values. The \`nonlocal\` keyword is required only when rebinding the outer name, not when mutating an object referenced by it. \`functools.wraps\` copies \`__name__\`, \`__doc__\`, and \`__wrapped__\` from the original to the wrapper. Decorators have existed since Python 2.4 (PEP 318). \`nonlocal\` was added in Python 3 (PEP 3104). <a href="/language-bytecode-dis">See how LOAD_DEREF and closure cells appear in bytecode</a>.`,

  "language-type-hints": `Type hints are a contract for humans and static checkers. The runtime ignores nearly all of them. Since PEP 484 (Python 3.5) introduced optional static typing, the system has evolved through PEP 585 (built-in generics in 3.9), PEP 604 (union syntax with \`|\` in 3.10), and PEP 649 (deferred evaluation in 3.14). The distinction between \`Any\` and \`object\` is critical: \`Any\` disables static checking, while \`object\` forces explicit narrowing. \`isinstance()\` rejects parameterized generics like \`list[int]\` at runtime with \`TypeError\`. Use \`typing.get_type_hints()\` instead of accessing raw \`__annotations__\`, which may contain unresolved string literals under \`from __future__ import annotations\`. <a href="/language-parameters">Combine type hints with careful signature design</a>. <a href="/async-limits-type-hints">See async-specific type hints for coroutines and awaitables</a>.`,

  "language-bytecode-dis": `Two snippets can produce the same result while one emits twice the opcodes. Bytecode shows you why. CPython compiles source into a code object containing an instruction stream. \`dis.dis()\` decompiles that stream into opcodes like \`LOAD_FAST\`, \`LOAD_GLOBAL\`, and \`BINARY_OP\`. \`LOAD_FAST\` is a direct C array access by index into the localsplus array, the fastest variable access. \`LOAD_GLOBAL\` looks up the global namespace dict, which is slower. \`LOAD_DEREF\` reads from closure cells. The frame object exposes \`f_locals\` as a mapping view and \`f_lasti\` as the last executed instruction offset, used for tracing and exception handling. PEP 659 (Python 3.11) introduced adaptive specialization, where the interpreter replaces generic opcodes with type-specific versions at runtime.`,

  "language-dunder-methods": `Special methods are Python's protocol API. You write \`__eq__\`, \`__iter__\`, \`__repr__\`, and the language calls them automatically when you use \`==\`, \`for\`, or \`repr()\`. CPython looks up these methods on the type, not on the instance, using the MRO (method resolution order). This bypasses the normal \`__getattribute__\` lookup, so special methods defined on individual instances are invisible to the interpreter. When \`__eq__\` returns \`NotImplemented\`, Python tries the reflected operation on the other operand. \`__hash__\` and \`__eq__\` must be consistent: equal objects must hash equally. \`__bool__\` falls back to \`__len__\` if \`__bool__\` is not defined. \`__iter__\` falls back to \`__getitem__\` with sequential integer indices. <a href="/language-identity-equality">See how __eq__ drives the == operator</a>. <a href="/dict-hash-tables">Learn why hashability matters for dict keys</a>.`,

  "runtime-del-gc": `\`del\` in Python deletes names, not objects. The object survives as long as any reference to it remains. CPython uses reference counting as its primary memory management. Every \`PyObject*\` has an \`ob_refcnt\` field, incremented on reference acquisition and decremented on release. When the count reaches zero, the object is deallocated immediately. Cyclic garbage collection handles the remaining case: objects that reference each other but are no longer reachable from any root. Non-container objects like strings and integers are never tracked because they cannot form cycles. The \`gc\` module exposes collection control and debugging. \`__del__\` finalizers are unreliable for deterministic cleanup. Context managers via \`with\` are the correct pattern for resource management. <a href="/memory-tuples-lists">See how reference counting affects tuple and list memory</a>. <a href="/language-identity-equality">Understand how aliasing creates additional references</a>.`,

  "dict-hash-tables": `Python's dict is implemented as a compact hash table (Python 3.6+) that splits indices and entries into separate arrays. The indices array is sparse, storing hash-derived positions. The entries array is dense, storing key-value pairs in insertion order. This split layout saves about 58% memory compared to the old combined-table design. Insertion order is a language guarantee since Python 3.7 (PEP 468). Keys must be hashable: they implement \`__hash__\` and \`__eq__\` consistently. Collisions are resolved through perturbed pseudo-random probing. The load factor triggers resize at 2/3 occupancy. Key-sharing dictionaries (PEP 412) optimize instance \`__dict__\` by sharing the keys object across instances of the same class. <a href="/dict-setdefault">Use setdefault and defaultdict for common dict patterns</a>. <a href="/sets-membership-views">Compare dict layout with set hash tables</a>.`,

  "dict-setdefault": `The \`if key not in d: d[key] = []\` pattern performs two dict lookups. \`setdefault\` does it in one. But the default argument to \`setdefault\` is evaluated eagerly, before checking whether the key exists. For expensive defaults, that means wasted allocation on every existing-key access. \`collections.defaultdict\` solves this with lazy evaluation through \`__missing__\`, called only when the key is absent. However, \`defaultdict\` has its own subtlety: reading a missing key mutates the mapping. This matters when iterating or checking membership. The \`get()\`-then-assign pattern is an anti-pattern: it still performs two lookups. \`defaultdict(list)\` is usually the cleanest choice for grouping patterns. <a href="/dict-hash-tables">Learn how dict lookups work under the hood</a>. <a href="/memory-container-comparison">Compare dict memory costs with other containers</a>.`,

  "sets-membership-views": `Set membership is O(1) average case. List membership is O(n). That difference compounds fast as the collection grows. CPython implements sets as a combined hash table using \`_Py_setentry\` structs, each storing a key pointer and its cached hash. Unlike dicts with their split-table layout, sets store key and hash together. Probing uses a hybrid strategy: nine steps of linear probing first, then the same perturb algorithm as dicts. Resize triggers at approximately 60% fill, higher than dicts because sets handle collisions differently in the combined layout. Dict views (\`dict.keys()\`, \`dict.items()\`) support set operations like \`&\`, \`|\`, \`-\`, \`^\` directly without materializing a standalone set. \`frozenset\` provides an immutable, hashable set for use as dict keys. <a href="/dict-hash-tables">Compare set layout with dict compact hash tables</a>. <a href="/memory-container-comparison">See how set memory scales versus list and tuple</a>.`,

  "memory-container-comparison": `Picking the wrong container costs memory, speed, or both. \`list\` is a dynamic array of \`PyObject*\` pointers with overallocation for amortized O(1) append. \`tuple\` is a fixed array with exact allocation and no per-element overhead beyond the container itself. \`set\` is a hash table with combined entries, roughly 216 bytes empty, scaling with the number of elements plus sparseness from the load factor. \`array.array\` stores raw C values inline with no per-element \`PyObject\` headers. For 1000 doubles, a list uses about 32,000 bytes (container + 1000 float objects) while an \`array('d')\` uses about 8,000 bytes. Membership is O(1) for dict and set, O(n) for list and tuple. This page compares all four with real measurements. <a href="/memory-tuples-lists">Deep dive on tuple vs list memory</a>. <a href="/memory-list-alternatives">Explore array.array, deque, and memoryview alternatives</a>. <a href="/dict-hash-tables">See how dict memory compares</a>.`,

  "memory-list-alternatives": `Lists are flexible, and that flexibility has a cost. For numeric data, \`array.array\` stores raw C doubles directly, skipping per-element \`PyObject\` overhead entirely. For FIFO queues, \`collections.deque\` provides O(1) appends and pops from both ends, while \`list.pop(0)\` is O(n) because every remaining reference must shift in the contiguous array. For binary data and buffer protocols, \`memoryview\` provides zero-copy slices over underlying buffers. Generators produce values on demand instead of holding everything in memory at once. Each alternative trades generality for performance in a specific dimension. This page covers \`array\`, \`deque\`, \`memoryview\`, and generators with memory measurements and use-case guidance. <a href="/memory-container-comparison">Compare all container types side by side</a>. <a href="/memory-iterables">Understand container vs streaming iterables</a>.`,

  "memory-tuples-lists": `A tuple allocates exactly. A list overallocates. That extra capacity is the deliberate price of efficient append. CPython's \`PyListObject\` uses an overallocation formula of roughly 12.5% per resize, giving amortized O(1) append. \`PyTupleObject\` has no overallocation because tuples are immutable. The list struct stores a separate \`ob_item\` pointer to a dynamically allocated array of \`PyObject*\` pointers. The tuple struct embeds \`ob_item[]\` inline after the header. For 10 floats, a list container is about 16 bytes larger than a tuple. This gap grows with the number of elements. Tuple copy elision: \`tuple(t)\` returns the same object when \`t\` is already a tuple. Tuples are hashable only when all elements are hashable. <a href="/memory-container-comparison">See how tuples compare with list and array.array in real measurements</a>. <a href="/classes-data-builders">Use namedtuple and NamedTuple for typed records</a>.`,

  "memory-iterables": `A list holds everything in memory. A generator produces each value on demand. Python distinguishes three categories: container sequences like \`list\` and \`tuple\` that store references to objects, flat sequences like \`array.array\` and \`bytes\` that store raw C values inline, and streaming iterables like generators that retain suspension frames and yield values lazily. Container sequences cost memory proportional to the number of elements plus their objects. Flat sequences pack values densely without per-element \`PyObject\` headers. Streaming iterables use constant memory per step regardless of sequence length. The generator attribute \`gi_yieldfrom\` tracks sub-iterators when using \`yield from\`. \`f_lasti\` on frames tracks the last executed instruction offset for suspension and resume. <a href="/memory-list-alternatives">Compare generators with array and deque for memory efficiency</a>. <a href="/async-iterators-generators">Learn about async generators for lazy streaming</a>.`,

  "classes-data-builders": `\`namedtuple\`, \`typing.NamedTuple\`, and \`@dataclass\` all build records. Each makes different tradeoffs. \`namedtuple\` creates a tuple subclass with named fields and tuple semantics: immutable, iterable, indexable, and hashable if all fields are hashable. \`typing.NamedTuple\` adds field annotations to the same tuple subclass. \`@dataclass\` creates an ordinary class with generated \`__init__\`, \`__repr__\`, \`__eq__\`, and optionally \`__order__\` and \`__hash__\`. Without \`slots=True\`, dataclasses store fields in \`__dict__\` like regular instances. Tuple subclasses use less memory per instance because they inherit the compact inline layout. This page covers memory measurements, field access patterns, and which builder fits each situation. <a href="/classes-dataclass-fields">Explore dataclass fields configuration in depth</a>. <a href="/memory-tuples-lists">See how namedtuple shares tuple memory characteristics</a>.`,

  "classes-dataclass-fields": `The \`@dataclass\` decorator generates \`__init__\`, \`__repr__\`, \`__eq__\`, and optionally ordering methods and \`__hash__\`. Field options control the generated behavior. \`default_factory\` solves the shared-mutable-default problem by calling a factory for each instance. \`frozen=True\` generates setter methods that raise \`FrozenInstanceError\`. \`order=True\` generates all comparison operators. \`slots=True\` (Python 3.10+) stores fields in \`__slots__\` descriptors, reducing memory per instance by eliminating \`__dict__\`. \`InitVar\` fields are passed to \`__init__\` but not stored, useful for computed initialization. \`ClassVar\` fields are excluded from all generated methods. Field ordering requires that fields without defaults come before fields with defaults. <a href="/classes-data-builders">Compare dataclass with NamedTuple and namedtuple</a>. <a href="/language-mutable-defaults">Understand the mutable default problem that default_factory solves</a>.`,

  "runtime-gil-performance": `The GIL (Global Interpreter Lock) is a single mutex in CPython that prevents two threads from executing Python bytecode simultaneously. It exists because CPython's memory management is not thread-safe by default. I/O-bound workloads release the GIL during blocking calls, so threads overlap effectively. CPU-bound workloads hold it, limiting parallelism to one thread at a time. \`ProcessPoolExecutor\` bypasses the GIL by using separate processes. Python 3.12 introduced per-interpreter GIL (PEP 684), where each sub-interpreter gets its own GIL. Python 3.13 added a free-threaded build mode (PEP 703) where the GIL can be disabled. Python 3.14 adds \`InterpreterPoolExecutor\` for running tasks across sub-interpreters. <a href="/async-foundations-awaitables">Compare GIL threading with async cooperative concurrency</a>. <a href="/asyncio-task-groups">See TaskGroup for structured async concurrency</a>.`,

  "async-foundations-awaitables": `\`await\` is a suspension point that pauses a coroutine so the event loop can run other tasks. Native coroutines, introduced in PEP 492 (Python 3.5), are defined with \`async def\`. Calling one creates a coroutine object without executing any code. Only \`await\` drives execution. CPython implements coroutines on the same frame-evaluation machinery as generators, using the \`RESUME\` opcode for suspension and resumption. The event loop schedules tasks cooperatively: a coroutine must reach an \`await\` point for other tasks to run. \`asyncio.run()\` (Python 3.7+) creates and manages the event loop. \`time.sleep()\` blocks the loop because it does not yield control. \`asyncio.sleep()\` is the correct non-blocking alternative. <a href="/async-context-backpressure">Learn backpressure and concurrency limiting for async code</a>. <a href="/async-iterators-generators">See async generators for lazy streaming</a>. <a href="/runtime-gil-performance">Compare async with threading and multiprocessing</a>.`,

  "async-context-backpressure": `Fan-out without a bound is a reliability incident waiting for a traffic spike. When you dispatch many async operations without limiting concurrency, you risk overwhelming downstream services or exhausting system resources. \`asyncio.Semaphore\` caps concurrent operations. \`asyncio.gather\` collects results with bounded fan-out, preserving input order in the result list. \`asyncio.as_completed\` reacts to results as they finish, returning results in completion order. \`asyncio.to_thread()\` moves blocking calls to a worker thread. For pure Python CPU work, the thread still contends for the GIL, but periodic GIL releases (every ~5ms) allow the event loop to regain control, preventing complete starvation. <a href="/async-foundations-awaitables">Start with async foundations if you are new to awaitables</a>. <a href="/asyncio-task-groups">Use TaskGroup for structured concurrency</a>. <a href="/async-limits-type-hints">Understand where async stops helping</a>.`,

  "async-servers-services": `\`asyncio.start_server\` creates a TCP server that accepts connections and spawns handler coroutines. Each handler receives \`asyncio.StreamReader\` and \`asyncio.StreamWriter\` objects. \`reader.readline()\` reads until a newline, \`writer.write()\` buffers data, and \`writer.drain()\` flushes and applies backpressure by suspending if the buffer exceeds the high-water mark. Clean shutdown requires \`writer.close()\` followed by \`await writer.wait_closed()\`. The server lifecycle uses \`async with server\` or \`serve_forever()\`. Blocking calls inside handlers freeze the entire loop. The handler is where you read, process, and respond. <a href="/async-context-backpressure">Learn backpressure patterns for server handlers</a>. <a href="/async-foundations-awaitables">Review awaitable basics before building servers</a>.`,

  "async-iterators-generators": `An async generator (PEP 525, Python 3.6+) produces values lazily while keeping the event loop responsive between items. Defined with \`async def\` and \`yield\`, it implements \`__aiter__\` and \`__anext__\` for use with \`async for\`. Async comprehensions (PEP 530, Python 3.6+) use \`async for\` inside list and set displays. Internally, CPython suspends the generator at each \`yield\` and resumes on the next \`__anext__\` call, allowing other tasks to run between yields. This is cooperative, not parallel: values arrive one at a time, but the loop does not block the event loop while waiting for each one. <a href="/async-foundations-awaitables">Learn the awaitable model that drives async generators</a>. <a href="/memory-iterables">Compare async generators with regular generators and iterables</a>.`,

  "async-limits-type-hints": `Async helps with I/O latency. CPU work still needs a separate parallelism strategy. In cooperative concurrency, a coroutine must reach an \`await\` point for other tasks to run. Pure CPU work has no suspension points, so it blocks the event loop regardless of async usage. \`time.sleep()\` blocks the loop. \`asyncio.to_thread()\` moves blocking work off the loop but pure Python CPU still contends on the GIL. The \`sys.getswitchinterval\` setting for thread switching has no effect on coroutines. For typing async boundaries, Python provides \`Awaitable[T]\`, \`AsyncIterable[T]\`, \`AsyncIterator[T]\`, and \`AsyncGenerator[T, SendT]\` in \`collections.abc\`. <a href="/async-foundations-awaitables">Master awaitable basics first</a>. <a href="/runtime-gil-performance">Understand GIL limits that affect to_thread</a>. <a href="/language-type-hints">General type hinting patterns for async code</a>.`,

  "asyncio-task-groups": `\`TaskGroup\` (PEP 654, Python 3.11) provides structured concurrency: if one task raises an unhandled exception, all sibling tasks are cancelled, and an \`ExceptionGroup\` is raised once all tasks complete. This is fundamentally different from \`asyncio.gather()\`, which collects results or individual exceptions but does not cancel siblings on failure. \`TaskGroup\` uses an \`async with\` block as the failure boundary. Tasks created within the group are owned by it. On exit, the group awaits all tasks. On exception, it cancels remaining tasks. For concurrency limits, pair \`TaskGroup\` with \`asyncio.Semaphore\`. For blocking work, use \`asyncio.to_thread()\`. <a href="/async-context-backpressure">Learn backpressure and semaphore patterns</a>. <a href="/async-foundations-awaitables">Review awaitable basics first</a>. <a href="/runtime-gil-performance">Understand threading vs async tradeoffs</a>.`,

  "production-stdlib-logging": `\`print()\` works until you need levels, routing, or structured output. Python's standard \`logging\` module provides hierarchical loggers, configurable levels (\`DEBUG\`, \`INFO\`, \`WARNING\`, \`ERROR\`, \`CRITICAL\`), multiple handlers (console, file, rotating, HTTP), and formatters. The \`extra\` parameter attaches structured context to log records. \`logger.exception()\` is equivalent to \`logger.error(exc_info=True)\`, logging the full traceback. Lazy formatting with \`%s\` style avoids string construction when the log level discards the message. Loggers follow a parent-child hierarchy: child loggers propagate to parents by default. This page covers configuration patterns, context enrichment, and safe exception logging. <a href="/async-context-backpressure">Apply logging patterns to async services</a>. <a href="/async-servers-services">Add logging to async server handlers</a>.`,
}
